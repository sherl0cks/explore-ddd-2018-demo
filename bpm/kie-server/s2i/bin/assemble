#!/bin/bash

set -e

shopt -s dotglob

if [ "$(ls -A /tmp/src)" ]; then
    echo "moving files to kie server..."
    mv /tmp/src/* $JBOSS_HOME/standalone/deployments/kie-server.war/WEB-INF/lib/
else
    echo "no files provided in binary build"    
fi

echo "build complete"